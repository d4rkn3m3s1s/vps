[Service]
# ★2026-07-28 KOK-FIX: agent'in cgroup'u SADECE agent'i degil, spawn ettigi TUM Waydroid
# oturum-sureclerini de tutuyor (weston + dnsmasq + dbus + bash/sleep per instance; 35
# cihazda 132 surec). Eski 1500M tavani doldu -> memory.failcnt ~5.000.000, oom_kill=10 ->
# agent'in her fork/exec'i (lxc-attach/bash/adb) bellek-baskisinda DUSUYOR -> tum .catch()
# yollari sessizce "route YOK"/bos donuyor -> heal ne kontrol edebiliyor ne route ekleyebiliyor
# (spam sonsuz) VE provision persist'i "cikis dogrulanamadi" veriyor. Host'ta 204GB bos var.
# Tavan guvenlik icin KALIYOR ama gercek ihtiyaca gore (35 cihaz ~1.4GB, 80 cihaz ~3GB) genis.
MemoryMax=16G
MemoryHigh=12G
