[Service]
# ★★★2026-08-22 AYNI TUZAK: servis dusunce BASLATTIKLARINI oldurmesin.
# wd-boot-toparla reboot sonrasi filoyu toparlar: wd-kademeli'yi DOGRUDAN cagirir
# (yani ayni cgroup'ta calisir), fleet-agent'i ve health-watch timer'ini baslatir.
# Varsayilan KillMode=control-group ile servis herhangi bir sebeple failed olursa
# systemd cgroup'taki her seye SIGTERM yollar — yani BOOT KURTARMA SERVISI, az once
# ayaga kaldirdigi seyleri oldurebilir. Kurtarma sisteminin kendisi icin bu kabul edilemez.
#
# CANLI KANIT (22 Agu 00:43): kardes servis wd-proxy-restore isini BITIRDIGI HALDE
# exit 1 dondu; systemd failed sayip cgroup'u temizledi ve az once baslatilan
# 141 redsocks SIGTERM aldi -> 102 cihaz "disari cikamiyor" durumuna dustu.
# (Ayni tuzak 29 Tem'de de yasanmisti.) wd-proxy-restore KillMode=process yapildi;
# bu ayni korumayi boot kurtarma zincirine de uygular.
KillMode=process
