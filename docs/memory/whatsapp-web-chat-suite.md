---
name: whatsapp-web-chat-suite
description: "WhatsApp Web tarzı sohbet arayüzü (sol liste + sağ chat), kategoriler/etiketler, 200+ sohbet için sayfalama + Telegram bot sohbet gezinme; kod tamam+tsc temiz, migration DB'ye UYGULANMADI"
metadata: 
  node_type: memory
  type: project
  originSessionId: d65a94e8-4747-4ce8-8084-8707ca12361d
---

2026-07-05 SESSION — WhatsApp API'yi "WhatsApp Web gibi" sohbet arayüzüne çevirdik + Telegram botu sohbet gezinmeyle geliştirdik. Kullanıcı hedefi: "yeri gelecek 200 sohbet olacak", kategoriler olsun. Kararlar: tek cihaz + sol sohbet listesi, manuel renkli etiketler + akıllı filtreler, Telegram da liste+kategori+sayfalama.

**YENİ MİMARİ — conversation katmanı (WhatsappMessage log'unun ÜSTÜNDE):**
- Prisma: `WhatsappConversation` (deviceId+peer @unique, lastMessageBody[şifreli], lastDirection, lastMessageAt, unreadCount, favorite, archived, labelIds[]) + `WhatsappLabel` (workspace etiket: name+color) + `WhatsappMessage.read` alanı + yeni index'ler. Backend'de [[whatsapp-public-api-suite]] üzerine kuruldu.
- Tek write path: `whatsapp.service.ts::recordMessage` — `agent.service.ts`'in İKİ mesaj yazma noktasına bağlandı (OUT: satır ~150 send-complete, IN: satır ~inboundWhatsapp). plainBody verilir, servis içeride encryptString'ler. IN → unreadCount++, archived=false; OUT → unread'a dokunmaz. **agent.mjs DEĞİŞMEDİ** (upsert tamamen API'de, mevcut /agent/whatsapp/inbound POST'una bağlı).

**DOSYALAR:**
- `apps/api/src/modules/whatsapp/` (YENİ): service (recordMessage, listConversations[filter/label/search/cursor], getThreadMessages[before-cursor], markRead, setConversationState[fav/arch], setConversationLabels, label CRUD) + controller + routes. Mount: routes/index.ts `/whatsapp`.
- Public API'ye eklendi: `/public/v1/whatsapp/conversations` + `/thread` (public.controller/routes).
- Dashboard proxy: `apps/dashboard/src/app/api/whatsapp/*` (conversations, /unread, /state, /labels, thread, thread/read, labels CRUD).
- `WhatsappView.tsx` TAMAMEN yeniden yazıldı: sol sidebar (arama+akıllı filtre Tümü/Okunmamış/Favori/Arşiv + etiket chip + sohbet listesi unread rozet/preview/label chip + "daha fazla" cursor) + sağ chat pane (balonlar, scroll-up eski mesaj, inline cevap Enter-gönder, header'da favori/etiket-dropdown/arşiv). Register paneli collapsible'a küçültüldü. Canlı: `useFleetEvents(['whatsapp.message'])` (live.tsx union'a eklendi).
- `globals.css`: `.wa-web/.wa-sidebar/.wa-conv/.wa-chat/.wa-msg/...` WhatsApp yeşili tema. ESKI `.wa-grid/.wa-bubble/.wa-messages` stilleri hâlâ duruyor (artık kullanılmıyor, silinebilir).
- `telegram.service.ts`: /sohbetler → cihaz seç → sayfalanmış inline sohbet listesi (peer'lar c:<index> ile referans, callback_data 64-byte limiti için pagePeers[]), filtre chip (cf:), etiket chip (cl:<index>/cl:x), cnext/cprev cursorStack paging, sohbet aç→geçmiş+markRead, reply (awaiting_reply mode → peer'a direkt yanıt), favtoggle/archtoggle. MAIN_MENU'de "Mesajları Oku" → "Sohbetler" oldu.
- **KOMUT PALETİ (2. tur, 2026-07-05):** `setMyCommands` + `setChatMenuButton{type:commands}` → Telegram'ın "/" slash-menüsü + sol-alt Menu butonu artık dolu. `ensureCommands(token)` pollBot başında token-başına-bir-kez (commandsRegistered Set, hata olursa retry). BOT_COMMANDS: menu/sohbetler/gonder/okunmamis/favoriler/etiketler/cihazlar/durum/yardim. Yeni komutlar+handler: /okunmamis+/favoriler (→ devicePickerButtons filter suffix `chatpick:<id>:<filter>`), /etiketler (renderLabels salt-okunur liste; oluşturma panelde), /durum (renderStatus: cihaz online + toplam okunmamış). MAIN_MENU 4 satıra çıktı (q:unread/q:favorite/labels/status callback'leri eklendi). NOT: Telegram komut adları [a-z0-9_] — Türkçe 'ı' YOK, o yüzden 'okunmamis'.

- **PRO UPGRADE (3. tur, 2026-07-05, "çok çok geliştir"):** 3 keşif ajanı → önceliklendirme → 3 dalga (biri Telegram fork'ta paralel). Migration `20260705140000_whatsapp_pro`.
  - **Şema:** `WhatsappMessage`: status(QUEUED/SENT/DELIVERED/READ/FAILED)+statusAt+failReason+dedupeKey(@unique) + index(workspaceId,direction,createdAt). `WhatsappConversation`: lastStatus+pinned+pinnedAt+displayName(düz)+notes(şifreli) + composite index + **GIN index(labelIds)**. Yeni model: `WhatsappCannedReply`(hazır cevap), `WhatsappBroadcast`(toplu). WebhookEvent'e WHATSAPP_SENT/WHATSAPP_FAILED.
  - **whatsapp.service:** recordMessage'a status; listConversations'a **pin sıralaması** (ilk sayfada pinned ayrı çekilip başa) + displayName'de arama; setContactInfo/getContactInfo(not şifreli); bulkAction(read/archive/fav/pin/label toplu); canned CRUD; getStats(gelen/giden/fail/açık/ort-ilk-yanıt IN→OUT eşleştirme); createBroadcast/listBroadcasts (jitter'lı arka-plan fan-out, createJobRecord ile). **BUG FIX:** deleteLabel artık workspace-scoped WHERE (eskiden tüm tenant'ları tarıyordu).
  - **agent.service:** WHATSAPP_SEND artık BAŞARISIZ gönderimleri de FAILED status+failReason ile kaydediyor (eskiden sessizce kayboluyordu) + WHATSAPP_SENT/FAILED webhook + broadcast failCount. inboundWhatsapp'a **dedupeKey idempotency** (P2002 → stored:false, fan-out'tan önce çıkar → mükerrer unread/bildirim yok).
  - **Dashboard WhatsappView (zengin):** yeni sohbet başlat, hazır cevap popover+yönetici, emoji picker, kişi bilgi paneli (isim+not), pin, mesaj kopyala/ilet (hover+modal), toplu seçim+bulk bar, broadcast modal, istatistik şeridi, gönderim durumu tikleri (✓/✓✓/mavi/⚠), klavye kısayolları (Esc/Ctrl+K/Ctrl+E). +6 proxy route (contact/bulk/canned/stats/broadcast).
  - **Telegram (FORK yaptı):** gelen bildirime inline [Cevapla/Aç/Okundu] butonu (wr:/wo:/wk:<msgId>, notifications.service'e telegramButtons param), /ara, /istatistik, hazır cevap gönder(cr:), thread'den etiket ata(setlbl:)+pin(pintoggle), çoklu chatId (virgülle ayır, izinli-liste). notifications.service+agent.service inbound fan-out değişti.

- **CANLI DEPLOY + PEER BUG FIX (2026-07-05, Scaleway):** 3 migration + pro paketi canlıya alındı, doğrulandı. **BUG: aynı numara ayrı sohbet** — inbound peer WhatsApp biçiminde ("+90 546 402 28 35"), outbound biçimi "905..." → 2 thread. FIX: `whatsapp.service.normalizePeer()` (rakam-benzeriyse sadece-rakam + baştaki 00 at; isim-tabanlı peer'ı koru) — recordMessage upsert-key + getThreadMessages + markRead/state/labels/contact/bulk + agent.service inbound&outbound yazma (dedupeKey de normalize peer'la). Migration `20260705160000_whatsapp_peer_normalize`: mesaj peer'larını normalize + mükerrer conversation'ları birleştir (DELETE-önce-UPDATE-sonra; İLK versiyonda UPDATE-önce unique çakıştı→resolve --rolled-back + düzelt). Canlı: 4 sohbet→3, "+90 546..." birleşti. **API-KEY KATEGORİ ("hepsi"):** public API'ye /public/v1/whatsapp/labels (GET+POST write), /conversations/labels (POST write), /conversations/state (POST write) eklendi; scope sistemi (read serbest/write-admin) zaten API-key kategorisi. NOT: WS ws-token 401 = login olmadan sayfa açınca normal, bug değil. DEPLOY YÖNTEMİ: rsync apps/api → köke değil apps/api'ye; package.json değişmediyse npm ci GEREKMEZ sadece prisma generate; migrate deploy; npm run build; systemctl restart fleet-api. Login brute-force kilidi test denemeleriyle tetikleniyor → `systemctl restart fleet-api` temizler.

- **API DOKÜMAN + PLAYGROUND GELİŞTİRME (2026-07-05, CANLI):** `apps/dashboard/src/app/admin/api-keys/page.tsx` — doküman eskiden sadece 3 uç anlatıyordu (devices/send/messages), şimdi **11 ucun hepsi** kategorili (Cihazlar/Sohbetler[conversations+thread]/Mesaj/Kategoriler[labels GET+POST+conversations-labels]/Sohbet durumu[state]/Toplu[broadcast]/İstatistik[stats]/Webhook[MESSAGE/SENT/FAILED]) + endpoint index + her uç parametre/curl/örnek-yanıt + yeni alanlar (pinned/displayName/lastStatus/status/failReason) + scope rozetleri. **Playground** deklaratif ENDPOINTS katalogu + `needs[]` bazlı dinamik form → 11 ucun HEPSİ test edilebilir (public-test proxy zaten `/public/*` forward ediyordu, sadece FE genişledi). **globals.css'e EKSİK api-doc-* stilleri EKLENDİ** (api-doc-verb/get/post/meta/callout/index/cat/scope/params/req/opt/errors — bunlar hiç tanımlı değildi, eski doküman stilsizdi). Canlı uçtan-uca doğrulandı: write-key oluştur→playground proxy conversations(pinned/lastStatus alanlarıyla)+stats(avgResponseMinutes:103)+labels-create hepsi 200/201.

**DURUM:** apps/api + apps/dashboard `tsc --noEmit` İKİSİ DE TEMİZ (3 dalga birleşik). **İKİ migration DB'ye UYGULANMADI** (`..120000_whatsapp_conversations` + `..140000_whatsapp_pro`; yerel PG kapalı localhost:5432; canlı Scaleway 51.158.107.121 — bkz [[production-deploy-scaleway]]). Deploy'da `prisma migrate deploy` ŞART. İkisi de idempotent. Commit'lenmedi; whatsapp/public/telegram klasörleri git'te untracked (??).
